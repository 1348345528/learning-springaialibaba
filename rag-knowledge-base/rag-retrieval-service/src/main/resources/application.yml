server:
  port: 8081
spring:
  ai:
    dashscope:
      api-key: sk-9259cd2c369d4dfaa44ce71fd8b9f67e
      embedding:
        options:
          model: text-embedding-v4
  datasource:
    url: jdbc:mysql://localhost:3306/rag_knowledgebase?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

milvus:
  host: localhost
  port: 19530
  collection: rag_chunks
  dimension: 1024

reranker:
  model: gte-rerank

hyde:
  model: qwen-plus

jwt:
  secret: mySecretKeyForJWTTokenGenerationAndValidation2026!Secure
